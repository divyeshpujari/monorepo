'use strict';
const SecurityScanResult = require('./SecurityScanResult');

/**
 * @module Schema
 * @description - Expose the mongoose schema over the system
 */
module.exports = {
  SecurityScanResult
};